One directory per published version, e.g. `1.0.0/`, holding the channel files
exactly as published (`latest.yml`, `latest-mac.yml` when a signed mac build
exists) and `RELEASE.md` with the notes. The artifacts themselves are the
release's assets on GitHub, not files here.
